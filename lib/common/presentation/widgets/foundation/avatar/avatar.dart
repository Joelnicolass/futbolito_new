import 'package:flutter/widgets.dart';

class AvatarFoundation extends StatelessWidget {
  const AvatarFoundation({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
